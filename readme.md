### Developers
Steps to get started:

```
brew install node
brew install watchman
npm install -g react-native-cli

react-native init <app name: aka lister>
```

Guide from https://www.raywenderlich.com/126063/react-native-tutorial
React Native Tutorial
